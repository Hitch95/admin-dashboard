<script>
	// ============================================
	// PARTIE 18 - TP : Exercice Theme
	// ============================================

	import { Button } from '$lib/components/ui/button/index.js';

	// TODO 2 : Importer toggleMode et mode depuis 'mode-watcher'
	//   import { toggleMode, mode } from 'mode-watcher';
	//   Importer les icônes Sun et Moon depuis 'lucide-svelte'
	//   import { Sun, Moon } from 'lucide-svelte';

	// TODO 3 : Créer un $derived pour l'icône à afficher :
	//   let icon = $derived($mode === 'light' ? Sun : Moon);
	//   (Sun si le mode est 'light', Moon sinon)
</script>

<!-- TODO 4 : Construire un Button variant="ghost" size="icon"
              qui appelle toggleMode() au clic et affiche l'icône dérivée -->
<!--
<Button variant="ghost" size="icon" class="size-7" onclick={toggleMode}>
	<Icon class="size-4" />
	<span class="sr-only">Changer le thème</span>
</Button>
-->
